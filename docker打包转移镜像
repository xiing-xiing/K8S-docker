从镜像仓库下载镜像：
docker pull {镜像仓库}/{镜像}:{镜像版本}

保存镜像：
docker save -o  {镜像名}.tar.gz {镜像仓库}/{镜像}:{镜像版本}

传输镜像：
scp   -r -P {端口}  {镜像名}.tar.gz   {目标ip}:{目录}

加载镜像：
docker load  -i {镜像名}.tar.gz

上传镜像仓库：
docker push  {镜像仓库}/{镜像}:{镜像版本}
